\section{Data Structures} % (fold)

\subsection{Disjoint Set} % (fold)
\lstinputlisting{codes/data_structures/disjoint_set.cpp}
% subsection disjoint_set (end)

\subsection{Binary Indexed Tree} % (fold)

For indices from $1$ to $n$, it supports two operations: adding a certain value to a certain index ($O(\lg{n})$), summing the values of a certain range ($O(\lg{n})$).
\lstinputlisting{codes/data_structures/binary_indexed_tree.cpp}

\subsubsection{Inversion Number} % (fold)
\lstinputlisting{codes/data_structures/inversion_number.cpp}
% subsubsection inversion_number (end)

% subsection binary_indexed_tree (end)

\subsection{KD Tree} % (fold)
It can be built from a set of $n$ points ($O(n\lg{n})$), and supports two operations: adding a certain value to all points inside a certain rectangle ($O(\lg{n})$), summing the values of all points inside a certain rectangle ($O(\lg{n})$).
\lstinputlisting{codes/data_structures/kd_tree.cpp}
% subsection kd_tree (end)

\subsection{Link/cut Tree} % (fold)
\lstinputlisting{codes/data_structures/link_cut_tree.cpp}
% subsection link_cut_tree (end)

% section data_structures (end)
